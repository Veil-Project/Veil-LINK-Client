import React from 'react';
import { RouteComponentProps } from '@reach/router'

const Receive = (props: RouteComponentProps) => (
  <div className="flex items-center justify-center">
    Receive
  </div>
)

export default Receive
